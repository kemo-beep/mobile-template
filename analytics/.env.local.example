# Copy to .env.local

# Required: Backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:8787

# Optional: App URL for OAuth callbacks (defaults to same origin)
# NEXT_PUBLIC_APP_URL=http://localhost:3001
